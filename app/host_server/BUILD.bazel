licenses(["notice"])

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "host_server",
    srcs = ["main.cc"],
    deps = [
        "//kernel:kernel",
        "@fmt//:fmt",
        "@spdlog//:spdlog",
    ],
)
