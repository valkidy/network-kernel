licenses(["notice"])

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "example_client",
    srcs = ["main.cc"],
    deps = [
        "//kernel:kernel",
        "@fmt//:fmt",
        "@spdlog//:spdlog",
    ],
)
