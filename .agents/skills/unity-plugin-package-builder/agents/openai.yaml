interface:
  display_name: "Unity Package Builder"
  short_description: "Build and verify the Unity UPM package"
  default_prompt: "Use $unity-plugin-package-builder or /unity-package on allowed refs (feat-unity-plugin, dev-*, or CI v* tags) to align Unity API with the C++ kernel API, build macOS and Windows x86_64 native plugins, stage network_kernel assets and gameplay catalog bundle bytes, remove .DS_Store files, pack, smoke-check, add inferred release notes, and auto-commit eligible com.network-example.kernel UPM package changes."
