interface:
  display_name: "Unity Package Builder"
  short_description: "Build and verify the Unity UPM package"
  default_prompt: "Use $unity-plugin-package-builder or /unity-package to build, stage, pack, and smoke-check com.network-example.kernel as a Unity UPM .tgz package."
