licenses(["notice"])

load(":skeleton_asset_rules.bzl", "skeleton_asset")

package(default_visibility = ["//visibility:public"])

skeleton_asset(
    name = "simplified_monster_sim_v4",
    src = "raw/simplified_monster_sim_v4.glb",
    rig = "raw/simplified_monster_sim_v4.rig.yaml",
    asset_id = 1,
)

skeleton_asset(
    name = "rock_robot_biped_24u_v3",
    src = "raw/rock_robot_biped_24u_v3.glb",
    asset_id = 2,
)

# Base locomotion templates, exported from LocomotionModel.unity by
# //tools:unity_scene_to_glb with no coordinate conversion. simplified_monster_sim_v4
# stays registered above until these have replaced it, because it is the only
# rig with a capture golden proving the locomotion system unchanged.
skeleton_asset(
    name = "simplified_biped",
    src = "raw/simplified_biped.glb",
    rig = "raw/simplified_biped.rig.yaml",
    asset_id = 3,
)

skeleton_asset(
    name = "simplified_quadruped",
    src = "raw/simplified_quadruped.glb",
    rig = "raw/simplified_quadruped.rig.yaml",
    asset_id = 4,
)

skeleton_asset(
    name = "simplified_tripod",
    src = "raw/simplified_tripod.glb",
    rig = "raw/simplified_tripod.rig.yaml",
    asset_id = 5,
)
